// TODO: Generate cliffy command modules.

const diagnostics = project.getPreEmitDiagnostics();

console.log(project.formatDiagnosticsWithColorAndContext(diagnostics));

// TODO: Create a base component for generating TypeScript code in a type-safe manner.

// TODO: Keep track of which dependencies to use for given modules that use third-party libraries.
