import { assertEquals } from "../../../deps/std/testing.ts";

import { T } from "./t.ts";

// TODO: Generate tests for each LexiconAliasLayer method.
Deno.test("TODO", () => {
  assertEquals(T, T);
});
